---
title: "React Native lint rules · uf"
description: "Platform-specific modules."
---

{/* Generated from crates/uf_lint/rules/react-native by `UF_BLESS=1 cargo test -p uf_lint --lib rule_docs`. Edit the examples there, not this page. */}

<p className="eyebrow">Lint rules</p>

# React Native

<div className="lede">
Platform-specific modules.
</div>

Every example on this page is run by uf's test suite. A Bad example
reports exactly the findings shown under it and a Good example reports
none. [All rules](/reference/lint).

## react-native/platform-split

Default: `warn`. Prefer platform-specific files over `Platform.OS` branches.

React Native picks `Button.ios.js` or `Button.android.js` at build time, so each platform ships only its own code. A `Platform.OS` branch ships both and decides at run time.

### Bad

`app/Button.js`

```js
// @flow
import { Platform } from "react-native";

export const radius: number = Platform.OS === "ios" ? 8 : 2;
```

`uf lint` reports:

```text
app/Button.js:4:31 prefer platform-specific files for React Native platform branches
```

### Good

`app/Button.ios.js`

```js
// @flow
export const radius: number = 8;
```

`app/Button.android.js`

```js
// @flow
export const radius: number = 2;
```
