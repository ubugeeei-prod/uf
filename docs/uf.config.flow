// @flow
import { defineConfig } from "@uniflowed/config";

export default defineConfig({
  app: {
    router: {
      entry: "docs/app.flow",
      root: "docs/app",
    },
    rendering: {
      modes: ["ssg"],
      cache: {
        actions: false,
        data: false,
        fetch: false,
        route: false,
      },
    },
  },
  build: {
    entries: ["docs/app.flow"],
    outDir: "dist/docs",
    staticBuild: true,
  },
  docs: {
    enabled: true,
    app: "docs/app.flow",
    source: "docs",
    outDir: "dist/docs",
    staticBuild: true,
    deploy: "void",
  },
});
