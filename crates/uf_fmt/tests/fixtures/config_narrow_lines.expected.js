// @flow
render(
  alpha,
  beta,
  gamma,
  delta,
  epsilon,
  zeta
);
const short = [1, 2, 3];
const wide = {
  alpha: 1,
  beta: 2,
  gamma: 3,
  delta: 4
};
const kept = compute(oneArgumentThatIsVeryLongIndeed);
