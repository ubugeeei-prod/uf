// @flow
call(
  alpha,
  beta,
  gamma,
  delta,
  epsilon,
);
const object = {
  first: 1,
  second: 2,
  third: 3,
};
const chain = promise
  .then(onFulfilled)
  .catch(onRejected);
type Union = "alpha" | "beta" | "gamma";
function f(
  first: string,
  second: number,
): void {}
const jsx = (
  <Component
    first={1}
    second={2}
    third={3}
  />
);
const ternary = condition
  ? consequent
  : alternate;
