# The official React Compiler source and its matching fixture snapshots.
# upstream/react is a submodule at `commit`; Cargo builds its Rust crates
# directly. The same checkout supplies the upstream .expect.md snapshots.
# The front end is uf's Flow parser and Rust AST/scope adapter, not Babel.
# Move the gitlink and this pin together when updating the compiler.
crate      react_compiler
version    0.1.0
repository https://github.com/react/react.git
commit     ff7445e636429ab26ce3d388535a79c845d88c94
fixtures   compiler/packages/babel-plugin-react-compiler/src/__tests__/fixtures/compiler
