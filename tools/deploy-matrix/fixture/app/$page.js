// @flow
//
// `ssg`: a page with no parameters, prerendered by the build and answered from
// the files every target serves.

import * as React from "@uniflowed/react";

export default component Home() {
  return <h1>matrix home</h1>;
}
