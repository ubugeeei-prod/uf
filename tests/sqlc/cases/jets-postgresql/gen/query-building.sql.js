/**
 * Generated by uf's sqlc plugin from `query-building.sql` with sqlc v1.31.1.
 * Do not edit: change the SQL and run `uf sqlc generate`.
 *
 * @generated SignedSource<<50f248ee20e48c9778f355e3f8642563>>
 * @flow
 */

import type { Queryable } from "@uniflowed/sql";
import * as sql from "@uniflowed/sql";
import * as pg from "@uniflowed/sql/postgresql";
import type { Pilot } from "./models.js";

const countPilotsSql = "SELECT COUNT(*) FROM pilots";

export function countPilots(db: Queryable): Promise<bigint | null> {
  return sql.one(db, "CountPilots", countPilotsSql, [], 1, (row) => pg.int8.decode(row[0]));
}

const listPilotsSql = "SELECT id, name FROM pilots LIMIT 5";

export function listPilots(db: Queryable): Promise<Array<Pilot>> {
  return sql.many(db, "ListPilots", listPilotsSql, [], 2, (row) => ({
    id: pg.int4.decode(row[0]),
    name: pg.string.decode(row[1]),
  }));
}

export type DeletePilotArgs = {|
  readonly id: number,
|};

const deletePilotSql = "DELETE FROM pilots WHERE id = $1";

export function deletePilot(db: Queryable, args: DeletePilotArgs): Promise<void> {
  return sql.exec(db, "DeletePilot", deletePilotSql, [pg.int4.encode(args.id)]);
}
