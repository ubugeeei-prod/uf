/**
 * Generated by uf's sqlc plugin from the schema with sqlc v1.31.1.
 * Do not edit: change the SQL and run `uf sqlc generate`.
 *
 * @generated SignedSource<<d79947a28868a6abdfd11eba68069371>>
 * @flow
 */

import type { JsonValue } from "@uniflowed/sql";
import * as mysql from "@uniflowed/sql/mysql";

/** The `people_mood` enum. */
export type PeopleMood = "sad" | "ok" | "happy";

export const peopleMoodValues: $ReadOnlyArray<PeopleMood> = ["sad", "ok", "happy"];

export const peopleMoodCodec: mysql.Codec<PeopleMood> = mysql.enumeration(
  "people_mood",
  peopleMoodValues,
);

/** The `people_opts` enum. */
export type PeopleOpts = "a" | "b";

export const peopleOptsValues: $ReadOnlyArray<PeopleOpts> = ["a", "b"];

export const peopleOptsCodec: mysql.Codec<PeopleOpts> = mysql.enumeration(
  "people_opts",
  peopleOptsValues,
);

/** A row of `people`. */
export type Person = {|
  readonly id: bigint,
  readonly name: string,
  readonly age: number | null,
  readonly flag: boolean,
  readonly tiny: number | null,
  readonly price: string | null,
  readonly ratio: number,
  readonly born: string | null,
  readonly at: string | null,
  readonly ts: string,
  readonly t: string | null,
  readonly y: number | null,
  readonly data: JsonValue | null,
  readonly raw: Uint8Array | null,
  readonly bits: Uint8Array | null,
  readonly mood: PeopleMood,
  readonly opts: string | null,
  readonly big: bigint | null,
|};

/** A row of `pets`. */
export type Pet = {|
  readonly id: number,
  readonly ownerId: bigint,
  readonly name: string,
|};
