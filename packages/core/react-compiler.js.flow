// @flow

declare export type ReactCompilerMode = 'syntax';

declare export interface ReactCompilerConfig {
  +enabled: boolean,
  +mode: ReactCompilerMode,
}

declare export var syntaxMode: ReactCompilerConfig;
declare export function compiler(config?: $Shape<ReactCompilerConfig>): ReactCompilerConfig;
