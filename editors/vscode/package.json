{
  "name": "uf",
  "displayName": "uf",
  "description": "Flow diagnostics and type errors, formatting, quick fixes, hover, definitions and completion from the uf toolchain, over uf lsp.",
  "version": "0.0.0",
  "license": "MIT",
  "publisher": "uniflowed",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ubugeeei-prod/uf.git",
    "directory": "editors/vscode"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Linters",
    "Formatters"
  ],
  "main": "./src/extension.js",
  "activationEvents": [
    "workspaceContains:uf.config.js"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "uf starts the `uf` binary from this project's node_modules, which is code the workspace supplies."
    }
  },
  "contributes": {
    "languages": [
      {
        "id": "flow",
        "aliases": [
          "Flow",
          "flow"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "commands": [
      {
        "command": "uf.restartServer",
        "title": "Restart Language Server",
        "category": "uf"
      },
      {
        "command": "uf.showOutput",
        "title": "Show Language Server Log",
        "category": "uf"
      },
      {
        "command": "uf.configureWorkspace",
        "title": "Configure Workspace for Flow",
        "category": "uf"
      },
      {
        "command": "uf.showMenu",
        "title": "Show Status and Commands",
        "category": "uf"
      }
    ],
    "configuration": {
      "title": "uf",
      "properties": {
        "uf.server.path": {
          "type": "string",
          "default": "",
          "scope": "machine-overridable",
          "markdownDescription": "Path to the `uf` binary. Absolute, or relative to the workspace folder; `${workspaceFolder}` and a leading `~` are expanded. Leave empty to use `node_modules/.bin/uf` and then `PATH`. A path that is not there is reported rather than ignored."
        },
        "uf.formatOnSave": {
          "type": "boolean",
          "default": false,
          "scope": "resource",
          "markdownDescription": "Format Flow files with `uf fmt` (through the language server) when they are saved. Off by default; `editor.formatOnSave` with uf chosen as the default formatter does the same thing."
        },
        "uf.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "scope": "window",
          "description": "Log the JSON-RPC traffic between VS Code and `uf lsp` to the uf output channel."
        },
        "uf.workspace.disableBuiltinValidation": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "markdownDescription": "Configure Flow language mode and JavaScript validation once inside folders with uf.config.js. Flow mode uses uf highlighting and inferred hover types without the built-in TypeScript providers. Existing project values and custom associations are preserved. Turn this off to choose the language mode and settings yourself."
        }
      }
    },
    "grammars": [
      {
        "language": "flow",
        "scopeName": "source.js.flow",
        "path": "./syntaxes/flow.tmLanguage.json"
      },
      {
        "scopeName": "source.flow.injection.uf",
        "path": "./syntaxes/flow.injection.json",
        "injectTo": [
          "source.js",
          "source.js.jsx",
          "source.js.flow"
        ]
      }
    ]
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@vscode/test-electron": "3.1.0",
    "@vscode/vsce": "4.0.0",
    "ovsx": "1.2.0",
    "vscode-oniguruma": "2.0.1",
    "vscode-textmate": "9.3.2"
  }
}
